import React from 'react';
import Menu from '../components/Menu'

const Favs = ({ }) => {
    return (
        <div>
            <h2>Favs page</h2>
            <Menu/>
        </div>
    );
}

export default Favs;