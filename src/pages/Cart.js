import React from 'react';
import Menu from '../components/Menu'

const Cart = ({ }) => {
    return (
        <div>
            <h2>Cart page</h2>
            <Menu/>
        </div>
    );
}

export default Cart;